<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fbcq.be.mapper.UserMapper">
    <!-- 로그인 처리 -->
    <select id="select" parameterType="com.fbcq.be.dto.request.LoginRequest" resultType="com.fbcq.be.dto.response.LoginResponse">
        SELECT user_id AS userId, name
        FROM Users
        WHERE email = #{email} AND password = #{password};
    </select>

    <!-- 회원가입 처리 -->
    <insert id="create" parameterType="com.fbcq.be.dto.request.SignUpRequest">
        INSERT INTO Users (user_id, email, password, name, phone, birth_date, gender)
        VALUES (#{userId}, #{email}, #{password}, #{name}, #{phone}, #{birthDate}, #{gender});
    </insert>
</mapper>
